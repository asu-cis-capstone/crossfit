using System;

namespace WodstarMobileApp.Droid
{
	public class TimerUtil
	{
		//Should be either "forTime" or "forReps"
		private String timerType;
		//Set what state the timer is in - running, complete, not started, etc.
		private String timerState;

		public TimerUtil ()
		{

		}

		public void stopTimerButtonClickHandler(/*Need to pass a reference to a timer object? */ ) {
			//Stop the timer clock
			//Change the state of the timer
		}
	}
}

